const notepad = document.querySelector('.notepad')

const a = document.createElement('p');
const b = document.createTextNode('This is the going to be the contents of the text');


notepad.appendChild(a);
